- name: allowed-queries
  definition:
    queries:
      - name: MyQuery
        query: |
          query MyQuery {
            users {
              id
              first_name
              family_name
            }
          }
